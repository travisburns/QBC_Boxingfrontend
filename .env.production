# Production build config. NEXT_PUBLIC_* is inlined at BUILD time, so the
# frontend must be rebuilt/redeployed after any change here.
# Only PUBLIC values belong here — never secrets.

# Public URL of the deployed .NET backend (SmarterASP.NET site).
NEXT_PUBLIC_API_BASE_URL=https://eyesic36-001-site1.ftempurl.com
